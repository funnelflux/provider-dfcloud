apiVersion: network.dfcloud.m.funnelflux.pro/v1alpha1
kind: Network
metadata:
  name: example-network
spec:
  forProvider:
    name: example-network
    cidrBlock: "10.0.0.0/16"
    location:
      provider: gcp
      region: europe-west1
  providerConfigRef:
    kind: ProviderConfig
    name: default
